---
const { class: className, ...props} = Astro.props;
---
<div class:list={['grid-6', className]} {...props}><slot /></div>
<style>
  .grid-6 {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    grid-template-rows: repeat(1, 1fr);
    gap: var(--size-vertical-base-2x);
  }
</style>
