---
const { ...props } = Astro.props;
---
<h2><slot {...props} /></h2>
<style lang="scss">
  @use "sass:map";
  @use "../../styles/breakpoints" as breakpoints;
  @use "../../styles/typography" as typography;
  @use "../../styles/spacing" as spacing;

  h2 {
    @include typography.font("2xl", "sm", "bold");
    margin-bottom: spacing.space(4);
  }
</style>
